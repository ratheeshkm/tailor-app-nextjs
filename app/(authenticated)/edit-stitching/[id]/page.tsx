import EditStitchingClient from "../../../components/EditStitchingClient";

interface EditPageProps {
  params: { id: string };
}

export default function EditStitchingPage(_props: EditPageProps) {
  return <EditStitchingClient />;
}
